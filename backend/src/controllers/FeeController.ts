import { Response } from 'express';\nimport { AuthenticatedRequest, asyncHandler } from '../middleware/auth';\nimport FeeService from '../services/FeeService';\nimport { validateRequest } from '../utils/validation';\nimport { CreateFeeSchema, CreateFeePaymentSchema } from '../validators/schemas';\n\nexport class FeeController {\n  createFee = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const data = validateRequest(CreateFeeSchema, req.body);\n    const dueDate = new Date(data.dueDate);\n\n    const fee = await FeeService.createFee(req.user.schoolId, {\n      ...data,\n      dueDate,\n    });\n\n    res.status(201).json({\n      success: true,\n      message: 'Fee created successfully',\n      data: fee,\n    });\n  });\n\n  getFeesByClass = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { classId } = req.params;\n    const fees = await FeeService.getFeesByClass(req.user.schoolId, classId);\n\n    res.status(200).json({\n      success: true,\n      data: fees,\n    });\n  });\n\n  recordPayment = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const data = validateRequest(CreateFeePaymentSchema, req.body);\n\n    const payment = await FeeService.recordPayment(req.user.schoolId, {\n      ...data,\n      paymentDate: new Date(),\n    });\n\n    res.status(201).json({\n      success: true,\n      message: 'Payment recorded successfully',\n      data: payment,\n    });\n  });\n\n  getStudentPayments = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { studentId } = req.params;\n    const payments = await FeeService.getStudentPayments(req.user.schoolId, studentId);\n\n    res.status(200).json({\n      success: true,\n      data: payments,\n    });\n  });\n\n  getPendingPayments = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const payments = await FeeService.getPendingPayments(req.user.schoolId);\n\n    res.status(200).json({\n      success: true,\n      data: payments,\n    });\n  });\n}\n\nexport default new FeeController();\n