import { Response } from 'express';\nimport { AuthenticatedRequest, asyncHandler } from '../middleware/auth';\nimport AttendanceService from '../services/AttendanceService';\nimport { validateRequest } from '../utils/validation';\nimport { CreateAttendanceSchema } from '../validators/schemas';\n\nexport class AttendanceController {\n  mark = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const data = validateRequest(CreateAttendanceSchema, req.body);\n    const date = new Date(data.date);\n\n    const attendance = await AttendanceService.markAttendance(req.user.schoolId, {\n      ...data,\n      date,\n    });\n\n    res.status(201).json({\n      success: true,\n      message: 'Attendance marked successfully',\n      data: attendance,\n    });\n  });\n\n  getStudentAttendance = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { studentId } = req.params;\n    const { startDate, endDate } = req.query;\n\n    if (!startDate || !endDate) {\n      return res.status(400).json({\n        success: false,\n        message: 'startDate and endDate are required',\n      });\n    }\n\n    const attendance = await AttendanceService.getStudentAttendance(\n      req.user.schoolId,\n      studentId,\n      new Date(startDate as string),\n      new Date(endDate as string)\n    );\n\n    res.status(200).json({\n      success: true,\n      data: attendance,\n    });\n  });\n\n  getSectionAttendance = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { sectionId } = req.params;\n    const { date } = req.query;\n\n    if (!date) {\n      return res.status(400).json({\n        success: false,\n        message: 'date is required',\n      });\n    }\n\n    const attendance = await AttendanceService.getSectionAttendance(\n      req.user.schoolId,\n      sectionId,\n      new Date(date as string)\n    );\n\n    res.status(200).json({\n      success: true,\n      data: attendance,\n    });\n  });\n\n  getAttendancePercentage = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { studentId } = req.params;\n    const { startDate, endDate } = req.query;\n\n    if (!startDate || !endDate) {\n      return res.status(400).json({\n        success: false,\n        message: 'startDate and endDate are required',\n      });\n    }\n\n    const percentage = await AttendanceService.getAttendancePercentage(\n      req.user.schoolId,\n      studentId,\n      new Date(startDate as string),\n      new Date(endDate as string)\n    );\n\n    res.status(200).json({\n      success: true,\n      data: { percentage },\n    });\n  });\n}\n\nexport default new AttendanceController();\n