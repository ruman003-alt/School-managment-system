import { Response } from 'express';\nimport { AuthenticatedRequest, asyncHandler } from '../middleware/auth';\nimport SectionService from '../services/SectionService';\nimport { validateRequest } from '../utils/validation';\nimport { CreateSectionSchema } from '../validators/schemas';\n\nexport class SectionController {\n  create = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const data = validateRequest(CreateSectionSchema, req.body);\n    const section = await SectionService.createSection(req.user.schoolId, data);\n\n    res.status(201).json({\n      success: true,\n      message: 'Section created successfully',\n      data: section,\n    });\n  });\n\n  getById = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { id } = req.params;\n    const section = await SectionService.getSection(req.user.schoolId, id);\n\n    res.status(200).json({\n      success: true,\n      data: section,\n    });\n  });\n\n  listByClass = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { classId } = req.params;\n    const sections = await SectionService.listSectionsByClass(req.user.schoolId, classId);\n\n    res.status(200).json({\n      success: true,\n      data: sections,\n    });\n  });\n\n  list = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const sections = await SectionService.listAllSections(req.user.schoolId);\n\n    res.status(200).json({\n      success: true,\n      data: sections,\n    });\n  });\n\n  update = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { id } = req.params;\n    const data = validateRequest(CreateSectionSchema.partial(), req.body);\n    const section = await SectionService.updateSection(req.user.schoolId, id, data);\n\n    res.status(200).json({\n      success: true,\n      message: 'Section updated successfully',\n      data: section,\n    });\n  });\n}\n\nexport default new SectionController();\n