import { Response } from 'express';\nimport { AuthenticatedRequest, asyncHandler } from '../middleware/auth';\nimport ClassService from '../services/ClassService';\nimport { validateRequest } from '../utils/validation';\nimport { CreateClassSchema } from '../validators/schemas';\n\nexport class ClassController {\n  create = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const data = validateRequest(CreateClassSchema, req.body);\n    const classRecord = await ClassService.createClass(req.user.schoolId, data);\n\n    res.status(201).json({\n      success: true,\n      message: 'Class created successfully',\n      data: classRecord,\n    });\n  });\n\n  getById = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { id } = req.params;\n    const classRecord = await ClassService.getClass(req.user.schoolId, id);\n\n    res.status(200).json({\n      success: true,\n      data: classRecord,\n    });\n  });\n\n  list = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const classes = await ClassService.listClasses(req.user.schoolId);\n\n    res.status(200).json({\n      success: true,\n      data: classes,\n    });\n  });\n\n  update = asyncHandler(async (req: AuthenticatedRequest, res: Response) => {\n    if (!req.user) {\n      return res.status(401).json({\n        success: false,\n        message: 'User not authenticated',\n      });\n    }\n\n    const { id } = req.params;\n    const data = validateRequest(CreateClassSchema.partial(), req.body);\n    const classRecord = await ClassService.updateClass(req.user.schoolId, id, data);\n\n    res.status(200).json({\n      success: true,\n      message: 'Class updated successfully',\n      data: classRecord,\n    });\n  });\n}\n\nexport default new ClassController();\n