import { Router } from 'express';\nimport FeeController from '../controllers/FeeController';\nimport { authMiddleware, roleMiddleware } from '../middleware/auth';\n\nconst router = Router();\n\n// All fee routes require authentication\nrouter.use(authMiddleware);\n\n// Create fee (School admin or Super admin)\nrouter.post('/', roleMiddleware('SUPER_ADMIN', 'SCHOOL_ADMIN'), FeeController.createFee);\n\n// Get fees by class\nrouter.get('/class/:classId', FeeController.getFeesByClass);\n\n// Record fee payment\nrouter.post('/payment', roleMiddleware('SUPER_ADMIN', 'SCHOOL_ADMIN', 'ACCOUNTANT'), FeeController.recordPayment);\n\n// Get student payments\nrouter.get('/student/:studentId/payments', FeeController.getStudentPayments);\n\n// Get pending payments\nrouter.get('/pending', roleMiddleware('SUPER_ADMIN', 'SCHOOL_ADMIN', 'ACCOUNTANT'), FeeController.getPendingPayments);\n\nexport default router;\n